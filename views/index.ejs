<p>Iota is the premier hub for affordable vintage clothing. We source the best possible garments for you to add to your collection of fashionable pieces. All styles, all quality, all afforable.</p>
<hr>
<% if (allrecs.length > 0) {%>
  <% allrecs.forEach((recref, index) => { %>
    <product>
    <table>
      <tr> <td colspan="2"><%= recref.product_name %></td></tr>
      <tr> <td colspan="2"><%= recref.prod_image %></td></tr>
      <tr> <td colspan="2"><%= recref.description %></td></tr>
      <tr> <td><%= recref.price %></td></tr>
      <tr> <td><%= recref.size %></td></tr>
      <tr> <td><%= recref.sold %></td></tr>
        <td><a href='/product/<%= recref.id %>/show'>Details</a></td></tr>
    </table>
  </product>
  <% }); %>
  <% } else { %>
    <p>No products available.</p>

<% } %>